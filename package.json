{
  "name": "hg-tech-kb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rspress build",
    "dev": "rspress dev",
    "preview": "rspress preview",
    "serve": "rspress preview",
    "clean": "rimraf doc_build dist",
    "lint": "tsc --noEmit",
    "build:analyze": "rspress build --analyze"
  },
  "dependencies": {
    "rspress": "^1.40.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "rimraf": "^5.0.5"
  }
}
